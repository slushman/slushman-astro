---
import SiteTitle from 'component:SiteTitle';
import { checkIsHome } from 'utils';

const { url } = Astro.request;
const isHome = checkIsHome(url);
---
{isHome && (
  <h1 class="font-medium text-[1em]"><SiteTitle /></h1>
)}

{!isHome && (
  <p class="mb-0"><SiteTitle /></p>
)}
